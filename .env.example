# Nexus Entertainment 환경 변수 설정 예시
# 이 파일을 복사하여 .env.local 또는 .env.production으로 사용하세요

# ============================================
# 프론트엔드 환경 변수 (Vite)
# ============================================

# API 서버 URL (프론트엔드에서 사용)
VITE_API_URL=http://localhost:8000

# WebSocket 서버 URL
VITE_WS_URL=ws://localhost:8000

# ============================================
# 백엔드 환경 변수 (Python/FastAPI)
# ============================================

# CORS 허용 오리진 (쉼표로 구분)
# 예: CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5180

# 데이터베이스 경로 (선택사항, 기본값: backend/user_profiles.db)
# DB_PATH=backend/user_profiles.db

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 서버 포트 (기본값: 8000)
PORT=8000

# ============================================
# 프로덕션 설정
# ============================================

# 프로덕션 모드에서 API URL (실제 도메인으로 변경)
# VITE_API_URL=https://api.yourdomain.com
# VITE_WS_URL=wss://api.yourdomain.com

# ============================================
# 보안 설정 (향후 구현)
# ============================================

# JWT Secret Key (인증 시스템 구현 시 사용)
# JWT_SECRET_KEY=your-secret-key-here

# 암호화 키 (민감 데이터 암호화 시 사용)
# ENCRYPTION_KEY=your-encryption-key-here

# ============================================
# 물리 시뮬레이션 설정
# ============================================

# MuMax3 경로 (실시간 시뮬레이션 사용 시)
# MUMAX3_PATH=/usr/local/bin/mumax3

# MuMax3 실시간 계산 활성화 (기본: false, Pre-computed 사용)
# MUMAX3_REALTIME=false

# ============================================
# 생체신호 센서 설정
# ============================================

# EEG 센서 활성화
# ENABLE_EEG=false
# EEG_DEVICE=openbci  # openbci 또는 neurosky

# HRV 센서 활성화
# ENABLE_HRV=false
# HRV_DEVICE=polar  # polar 또는 fitbit

# 오디오 분석 활성화 (Web Audio API)
# ENABLE_AUDIO_ANALYSIS=false
